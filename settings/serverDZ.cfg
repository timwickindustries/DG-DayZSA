//steamPort = 2303;
steamQueryPort = 27013;
hostname = "DisturbedGaming - PVE | Hordes | https://discord.gg/pDszubW";  // Server name
//hostname = "GameTrackerClaimServer";
password = "";              // Password to connect to the server
passwordAdmin = "P@$$w0rd!53";         // Password to become a server admin
 
maxPlayers = 60;            // Maximum amount of players
 
equalModRequired = 0;
verifySignatures = 2;       // Verifies .pbos against .bisign files. (only 2 is supported)

forceSameBuild = 1;         // When enabled, the server will allow the connection only to clients with same the .exe revision as the server (value 0-1)
 
motd[] = {"Welcome to the Disturbed Gaming PVE Server!","Check out our Website at DisturbedGaming.org for our Teamspeak, Forum, and Discord! https://discord.gg/pDszubW"}; // Message of the day displayed in the in-game chat
motdInterval = 900;           // Time interval (in seconds) between each message

disableVoN = 0;             // Enable/disable voice over network (value 0-1)
vonCodecQuality = 7;        // Voice over network codec quality, the higher the better (values 0-30)
 
disable3rdPerson=0;         // Toggles the 3rd person view for players (value 0-1)
disableCrosshair=0;         // Toggles the cross-hair (value 0-1)
 
serverTime="2018/7/4/6/0";    // Initial in-game time of the server. "SystemTime" means the local time of the machine. Another possibility is to set the time to some value in "YYYY/MM/DD/HH/MM" format, f.e. "2015/4/8/17/23" .
serverTimeAcceleration=1;  // Accelerated Time (value 0-24)// This is a time multiplier for in-game time. In this case, the time would move 24 times faster than normal, so an entire day would pass in one hour.
serverTimePersistent=0;     // Persistent Time (value 0-1)// The actual server time is saved to storage, so when active, the next server start will use the saved time value.
 
guaranteedUpdates=1;        // Communication protocol used with game server (use only number 1)
 
loginQueueConcurrentPlayers=5;  // The number of players concurrently processed during the login process. Should prevent massive performance drop during connection when a lot of people are connecting at the same time.
loginQueueMaxPlayers=500;       // The maximum number of players that can wait in login queue
 
instanceId = 1;             // DayZ server instance id, to identify the number of instances per box and their storage folders with persistence files
lootHistory = 1;            // How many persistence history files should be kept by instance, number is looped over during save
storeHouseStateDisabled = false;// Disable houses/doors persistence (value true/false), usable in case of problems with persistence
storageAutoFix = 1;         // Checks if the persistence files are corrupted and replaces corrupted ones with empty ones (value 0-1)

// Kill Feed Config
killfeedMelee = 1;
killfeedRanged = 1;
killfeedSuicide = 1;
killfeedAnimal = 1;
killfeedInfected = 1;
killfeedBleedOut = 1;
killfeedShowFalls = 1;
killfeedUnknown = 1;
killfeedLogOverride = 1;
killfeedLogAnimal = 1;
killfeedLogInfected = 1;
killfeedLogUnknown = 1;
 
class Missions
{
    class DayZ
    {
        template="dayzOffline.chernarusplus"; // Mission to load on server startup. <MissionName>.<TerrainName>
    };
};